# Core dependencies (torch, numpy provided by ComfyUI)
safetensors>=0.4.0
huggingface_hub>=0.20.0
transformers>=4.45.0,<5.0.0
bitsandbytes>=0.44.0
einops>=0.8.0
torchaudio>=2.0.0
scipy>=1.10.0

# Audio I/O
soundfile>=0.12.0
librosa>=0.10.0
av>=12.0.0
pydub>=0.25.0
imageio-ffmpeg>=0.4.9  # bundles an ffmpeg binary so pydub/torchaudio have it

# Text encoding
sentencepiece>=0.2.0

# Duration estimation (Kokoro)
kokoro>=0.9.0

# Whisper validation (validate=True, !INTERRUPTION! marker in Dialogue Generate)
# Reuses whichever local whisper-small folder Voice Clone Loader points at
# (models/voice_clone/whisper-small/) via transformers — no separate model
# or format needed.

# MelBandRoFormer vocal separation
rotary-embedding-torch>=0.8.0

# LTX pipeline deps
openimageio>=3.0.0
tqdm>=4.60.0
Pillow>=9.0.0

# Model loading
accelerate>=0.30.0

# SeedVC voice conversion
munch>=4.0.0
descript-audio-codec>=1.0.0
hydra-core>=1.3.0
pyyaml>=6.0.0
python-dotenv>=1.0.0
diffusers>=0.37.0
onnxruntime>=1.16.0
funasr>=1.3.0
